/// This contains the "entrypoint" to the ags application.
///
///

import { RootComponent } from "./tree/component";

export const render = (config: {
    components: RootComponent[],
}) => {
    App.config({

    })
};
